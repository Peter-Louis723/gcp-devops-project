steps:
- name: 'docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/gcpdevops:$COMMIT_SHA', '.']
images:
  - 'gcr.io/$PROJECT_ID/gcpdevops'
serviceAccount: 'cloud-build-service-account@perfect-tape-487709-p0.iam.gserviceaccount.com'
# The fix
options:
  logging: CLOUD_LOGGING_ONLY